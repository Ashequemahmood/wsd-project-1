    <% layout("./layout/layout.eta") %>
    <% if (parseInt(it.shoppingLists) > 0) { %>
    
    <li>Shopping lists: <%= it.shoppingLists %></li>
    <li>Shopping list items: <%= it.shoppingListItems %></li>
    
    <% } else { %>
    <p>No shopping lists yet.</p>
    <% } %>
    
 